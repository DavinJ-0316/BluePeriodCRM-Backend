apps:
  - name: 'DevilsCRM'
    script: 'dist/index.js'
    node_args: '-r ts-node/register -r tsconfig-paths/register'
    kill_timeout: 10000
    watch: true
    exec_mode: cluster
    env:
      NODE_ENV: development
